{
  "name": "basepulse-ido-subgraph",
  "version": "1.0.0",
  "description": "The Graph subgraph for BasePulse IDO Factory",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy:sepolia": "graph deploy --node https://api.studio.thegraph.com/deploy/ bpulseido-sepolia",
    "deploy:mainnet": "graph deploy --node https://api.studio.thegraph.com/deploy/ bpulseido-mainnet",
    "create-local": "graph create --node http://localhost:8020/ bpulseido-sepolia",
    "remove-local": "graph remove --node http://localhost:8020/ bpulseido-sepolia",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 bpulseido-sepolia"
  },
  "devDependencies": {
    "@graphprotocol/graph-cli": "^0.56.0",
    "@graphprotocol/graph-ts": "^0.31.0"
  }
}
